pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'pip install --user -r requirements.txt '
                echo 'python -m virtualenv env'
                echo 'env \\Scripts\\activate'
                
            }
        }
    }
}
